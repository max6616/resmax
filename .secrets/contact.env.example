# Contact email advertised via the User-Agent header to polite APIs
# (OpenAlex, Crossref). Required by these providers' acceptable-use policies;
# supplying one unlocks the fast polite pool and avoids rate-limit throttling.
#
# Used by: .agents/skills/resmax-database/scripts/enrich_abstracts_fallback.py
#
# Put any email you are comfortable making public in API traffic logs.
export RESMAX_CONTACT_EMAIL=''
