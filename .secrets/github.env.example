# GitHub personal access token
# Used by: .agents/skills/resmax-database/scripts/enrich_code_quality.py
# Create at https://github.com/settings/tokens (scope: public_repo is enough)
# Without a token the GitHub API rate limit is 60 requests/hour.
export GITHUB_TOKEN=''
